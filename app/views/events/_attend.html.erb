<%= form_for(current_user.invites.build(attended_event_id: @event.id)) do |f| %>
  <%= f.hidden_field :attended_event_id, :value => @event.id %>
  <%= f.submit "Attend Event", class: "form-control btn btn-info center" %>
<% end %>
