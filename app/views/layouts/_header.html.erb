<div role='navigation' class='navbar navbar-default navbar-fixed-top'>
  <div class='container'>
	  <div class='navbar-header'>
	    <%= link_to "Events handler", "#", class: 'navbar-brand' %>
	  </div>
    <div class='navbar-collapse collapse'>
	    <ul class='nav navbar-nav navbar-right'>
		    <li class="active">
		      <%= link_to "Home", root_url %>
		    </li>
		    <li><%= link_to "Events", events_url %></li>
		    <% if logged_in? %>
		    <li class="dropdown">
			    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><span class="caret"></span></a>
			    <ul class="dropdown-menu">
			      <li class="dropdown-header">User</li>
			      <li><%= link_to "Profile", current_user %></li>
			      <li><%= link_to "My events" , current_user %></li>
			      <li><%= link_to "Create event", new_event_url %></li>
			      <li class="divider"></li>
			      <li><%= link_to "Log out", logout_path, method: :delete %></li>
		  	  </ul>
		    </li>
		    <% else %>
		    <li><%= link_to "Log in", login_path %></li>
		    <% end %>
	    </ul>
    </div>
  </div>
</div>
